server-port: 8087
server:
   port: 8888

spring:
   cloud:
      config:
         server:
            git:
               uri: https://github.com/RedS6X/springcloud.git
               search-paths: config-data
#security:
user:
 name: root
 password: s3cr3t